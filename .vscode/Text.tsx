console.log("");
import { StyleSheet, View } from "react-native";

interface TextProps {}

const Text = ({}: TextProps) => {
  return <View></View>;
};

const styles = StyleSheet.create({});

export default Text;
